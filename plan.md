# What
- Add a search for the nominations, this should allow users search for a participant's record, (name, id, etc).
- Default the nominations table to in-progress
- Ensure that allowances populate
